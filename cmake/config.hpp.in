// Generated by CMake from cmake/config.hpp.in. Do not edit.
//
// What this build actually contains. The point of generating it rather
// than testing for __APPLE__ and friends in the headers is that the answer
// is a *build* decision: a macOS build with GPPLUS_BACKEND_IOKIT=OFF is
// still macOS, and code that branches on the platform macro would be wrong
// about it.

#ifndef GPPLUS_CONFIG_HPP
#define GPPLUS_CONFIG_HPP

#define GPPLUS_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define GPPLUS_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define GPPLUS_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define GPPLUS_VERSION_STRING "@PROJECT_VERSION@"

/// The string this build matches in the database's `platform:` field.
#define GPPLUS_PLATFORM_NAME "@GPPLUS_PLATFORM_TAG@"

/// One per backend compiled in. Absent means the backend is not in this
/// binary at all -- its source was never compiled, not compiled and
/// disabled.
@GPPLUS_BACKEND_DEFINES@

/// Whether a mapping database was compiled in, and how many lines of it.
#define GPPLUS_HAS_BUILTIN_DB @GPPLUS_HAS_BUILTIN_DB@
#define GPPLUS_BUILTIN_DB_ENTRIES @GPPLUS_BUILTIN_DB_ENTRIES@

#endif // GPPLUS_CONFIG_HPP
